{
  "name": "pokehuta-backend",
  "private": true,
  "scripts": {
    "emulators": "firebase emulators:start --only firestore,storage,auth,functions"
  },
  "devDependencies": {
    "firebase-tools": "^15.28.2"
  }
}
